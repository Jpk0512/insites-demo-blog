# Traefik strips the path prefix, so the app sees /health at root.
server {
    listen 80;
    server_name _;
    location = /health { add_header Content-Type text/plain; return 200 "insites-demo-blog OK\n"; }
    location / { root /usr/share/nginx/html; index index.html; }
}
